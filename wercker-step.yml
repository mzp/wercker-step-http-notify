name: http-notify
version: 0.1.3
description: send http request to specified url(a.k.a. webhook)
keywords:
  - web hook
  - notification
  - http
properties:
  url:
    type: string
    required: true
